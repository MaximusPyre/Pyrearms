# PyreLink

Desktop peer-share client. MIT.

```bash
npm install
npm run tauri:dev
```

**Host** → go online → copy share codes.  
**Get** → paste a `pyrelink:1:…` code → fetch.

See [../docs/PROTOCOL.md](../docs/PROTOCOL.md).
